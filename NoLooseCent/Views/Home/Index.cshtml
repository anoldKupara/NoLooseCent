@model NoLooseCent.ViewModels.DashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
}

<h2 class="text-center mb-4">NoLooseCent Financial Dashboard</h2>

<!-- Action Buttons -->
<div class="d-flex justify-content-center mb-4">
    <a asp-controller="Income" asp-action="Create" class="btn btn-success me-3">
        <i class="fas fa-plus-circle"></i> Add Income
    </a>
    <a asp-controller="Expense" asp-action="Create" class="btn btn-danger">
        <i class="fas fa-minus-circle"></i> Add Expense
    </a>
</div>

<!-- USD Summary -->
<h4 class="mb-3">USD Summary</h4>
<div class="row text-center mb-4">
    <div class="col-md-4">
        <div class="card text-white bg-success shadow">
            <div class="card-body">
                <h5>Total USD Income</h5>
                <h3>@Model.TotalUsdIncome.ToString("C")</h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-white bg-danger shadow">
            <div class="card-body">
                <h5>Total USD Expense</h5>
                <h3>@Model.TotalUsdExpense.ToString("C")</h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-white bg-info shadow">
            <div class="card-body">
                <h5>USD Balance</h5>
                <h3>@Model.UsdBalance.ToString("C")</h3>
            </div>
        </div>
    </div>
</div>

<!-- ZWL Summary -->
<h4 class="mb-3">ZWL Summary</h4>
<div class="row text-center mb-4">
    <div class="col-md-4">
        <div class="card text-white bg-success shadow">
            <div class="card-body">
                <h5>Total ZWL Income</h5>
                <h3>@Model.TotalZwlIncome.ToString("N2") ZWL</h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-white bg-danger shadow">
            <div class="card-body">
                <h5>Total ZWL Expense</h5>
                <h3>@Model.TotalZwlExpense.ToString("N2") ZWL</h3>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card text-white bg-info shadow">
            <div class="card-body">
                <h5>ZWL Balance</h5>
                <h3>@Model.ZwlBalance.ToString("N2") ZWL</h3>
            </div>
        </div>
    </div>
</div>

<!-- Recent Transactions Table -->
<hr class="mb-4" />

<h4>Recent Transactions</h4>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Date</th>
            <th>Type</th>
            <th>Amount</th>
            <th>Currency</th>
            <th>Source / Purpose</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.RecentTransactions)
        {
            <tr>
                <td>@item.Date.ToString("yyyy-MM-dd")</td>
                <td>
                    <span class="badge @(item.Type == "Income" ? "bg-success" : "bg-danger")">@item.Type</span>
                </td>
                <td>@item.Amount.ToString("N2")</td>
                <td>@item.CurrencyName</td>
                <td>@item.SourceOrPurpose</td>
            </tr>
        }
    </tbody>
</table>